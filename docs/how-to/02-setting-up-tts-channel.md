# TTS - Setting Up The TTS Channel

By default, Lerche listens in a channel named `tts`.
Admins can change this with:

`/tts-channel-name channel-name`

Use the channel name only, without the `#`.

Example:

`/tts-channel-name bot-tts`

After that, Lerche will only read messages from `#bot-tts`.
